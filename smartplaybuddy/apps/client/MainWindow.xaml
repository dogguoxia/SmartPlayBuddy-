<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SmartPlayBuddy.Client.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SmartPlayBuddy Client">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="SmartPlayBuddy 客户端" Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Header="WebSocket URL" Text="{x:Bind ViewModel.WsUrl, Mode=TwoWay}" />
            <Button Grid.Column="1" Content="连接" Command="{x:Bind ViewModel.ConnectCommand}" VerticalAlignment="Bottom" />
            <Button Grid.Column="2" Content="断开" Command="{x:Bind ViewModel.DisconnectCommand}" VerticalAlignment="Bottom" />
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0"
                      Header="行为"
                      SelectedItem="{x:Bind ViewModel.SelectedMode, Mode=TwoWay}"
                      ItemsSource="{x:Bind ViewModel.Modes}"
                      VerticalAlignment="Bottom"
                      MinWidth="120" />
            <TextBox Grid.Column="1"
                     Header="Action"
                     Text="{x:Bind ViewModel.CommandAction, Mode=TwoWay}"
                     Visibility="{x:Bind ViewModel.IsTextMode, Mode=OneWay}" />
            <TextBox Grid.Column="2"
                     Header="Data (JSON)"
                     Text="{x:Bind ViewModel.CommandData, Mode=TwoWay}"
                     Visibility="{x:Bind ViewModel.IsTextMode, Mode=OneWay}" />
            <TextBlock Grid.Column="1" Grid.ColumnSpan="2"
                       Text="点击发送按钮截取当前屏幕并上传到服务端"
                       VerticalAlignment="Center"
                       Visibility="{x:Bind ViewModel.IsScreenshotMode, Mode=OneWay}" />
            <Button Grid.Column="3"
                    Content="发送"
                    Command="{x:Bind ViewModel.SendCommand}"
                    VerticalAlignment="Bottom" />
        </Grid>

        <Grid Grid.Row="3" RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0">
                <Run Text="状态: " />
                <Run Text="{x:Bind ViewModel.ConnectionStatus, Mode=OneWay}" />
            </TextBlock>
            <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Logs, Mode=OneWay}" />
        </Grid>
    </Grid>
</Window>
